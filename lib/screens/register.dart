import 'package:flutter/material.dart';

class RegsiterScreen extends StatelessWidget {
  const RegsiterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register')),
        body: Center(child: Text('Join VitalBoost'),),
      );
  }
}    